{
  "name": "hexalife",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "browserify main.js --standalone  hexa > dist/hexalife.js -t [ babelify --presets [ env ] ]"
  },
  "author": "Muhammed Furkan Gungor",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babelify": "^7.3.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.0"
  }
}
